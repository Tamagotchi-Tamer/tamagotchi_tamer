import 'package:flutter/material.dart';

class TTLoading extends StatelessWidget {
  const TTLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
